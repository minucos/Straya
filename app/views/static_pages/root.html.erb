<% if logged_in? %>
  <script type="text/javascript">
    window.currentUser = <%= render(
      "api/athletes/athlete.json.jbuilder",
      athlete: current_user
    ).html_safe %></script>
<% end %>

<script>
  window.images = {
    splash_image: "<%= asset_path("splash-image.jpg")%>",
    demo_profile_pic: "<%= asset_path("demouser-profile-pic.png")%>",
  }
</script>

<main id="root">react is broken</main>